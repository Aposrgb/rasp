{% extends 'base.html.twig' %}

{% block title %}Days{% endblock %}

{% block body %}
    <h1>Days</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ day.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('days_index') }}">back to list</a>

    <a href="{{ path('days_edit', {'id': day.id}) }}">edit</a>

    {{ include('days/_delete_form.html.twig') }}
{% endblock %}
